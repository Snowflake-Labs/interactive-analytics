# Snowflake connection name from ~/.snowflake/connections.toml
CONNECTION_NAME=DEFAULT

# Solution name — derives database and warehouse names 
# (default, generated by agent: IWB_<YYYYMMDDHH24MI>)
# the value here is just a fallback 
# Database: <SOLUTION_NAME>_DB
# Warehouse: <SOLUTION_NAME>_INT_WH
SOLUTION_NAME=IWBENCH

# Cache externalbrowser SSO tokens between server restarts (default: on).
# Authenticate once in the browser; later restarts reuse ~/.snowflake/credential_cache/.
# SNOWFLAKE_CREDENTIAL_CACHE_DIR=~/.snowflake/credential_cache
# SNOWFLAKE_CLIENT_STORE_TEMPORARY_CREDENTIAL=false

# TPC-H benchmark database (created by tpc-h/setup)
# Overrides the derived name (<SOLUTION_NAME>_DB)
# SNOWFLAKE_DATABASE=IW_TPCH_DB

# Optional: override derived warehouse/schema names
# INTERACTIVE_WAREHOUSE=IW_TPCH_INT_WH
# INTERACTIVE_SCHEMA=IW_TPCH_IT

# Connection pool size (concurrent Snowflake connections)
# POOL_SIZE=10

# Optional: explicit connection instead of CONNECTION_NAME
# SNOWFLAKE_ACCOUNT=your_account
# SNOWFLAKE_USER=your_user
# SNOWFLAKE_ROLE=SYSADMIN
# SNOWFLAKE_AUTHENTICATOR=externalbrowser
